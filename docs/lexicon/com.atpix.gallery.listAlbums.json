{
  "lexicon": 1,
  "id": "com.atpix.gallery.listAlbums",
  "defs": {
    "main": {
      "type": "query",
      "description": "List albums for an author, excluding unlisted/permissioned unless caller is authorized.",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "visibility": { "type": "ref", "ref": "com.atpix.gallery.defs#visibility" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 20 },
          "cursor": { "type": "string" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["albums"],
          "properties": {
            "cursor": { "type": "string" },
            "albums": {
              "type": "array",
              "items": { "type": "ref", "ref": "com.atpix.gallery.defs#albumView" }
            }
          }
        }
      }
    }
  }
}