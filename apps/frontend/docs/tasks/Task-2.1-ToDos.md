# Task 2.1 / FE-2.1 ToDos

- [ ] Branch `task/2.1-oauth-shell` created from `main`
- [ ] Unit tests for color scheme preference and router
- [ ] UI tests for shell chrome, theme toggle, responsive grid (production build)
- [ ] OAuth client factory with loopback/production client_id from metadata module
- [ ] `localStorage` key `atpix-color-scheme` with dark/light/system + `data-theme` on `<html>`
- [ ] Semantic badge colors unchanged between themes
- [ ] UI-SHELL-001 header tabs, sidebar identity, sign-out, upload actions
- [ ] UI-SHELL-002 responsive 2/3/4 column gallery placeholder grid
- [ ] Sign-in panel for unauthenticated users; shell after session restore
- [ ] OAuth callback path `/oauth/callback` via SPA fallback
- [ ] `npm run test:ui` runs `vite build --mode test` then vitest against `dist/`
- [ ] README "Test current functionality" updated for Task 2.1
- [ ] Check off Task 2.1 in `docs/plan.md` and FE-2.1 in `apps/frontend/docs/plan.md`
- [ ] Walkthrough with raw CLI output; delete Plan and ToDos
