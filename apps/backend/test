#!/usr/bin/env bash
# Run backend tests inside apps/backend/.venv (installs dev deps on first use).
set -euo pipefail

ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT"

if [[ ! -d .venv ]]; then
  python3 -m venv .venv
fi

.venv/bin/pip install -q -r requirements-dev.txt
exec .venv/bin/pytest "$@"